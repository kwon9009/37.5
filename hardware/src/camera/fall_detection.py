"""
낙상 감지 - 카메라(OV5647) + 오픈소스 비전 AI(자세추정) 기반.

접근 방식(추천):
  1) 경량 자세추정 모델로 사람의 관절(keypoints)을 뽑는다.
     - 후보: MediaPipe Pose(가벼움, 단일 인물), MoveNet(TFLite, 매우 빠름), YOLOv8n-pose(자료 풍부)
     - 라즈베리파이5 CPU에서는 FPS가 낮을 수 있음 -> 경량 모델(NCNN/TFLite) 또는 AI HAT 검토.
  2) 관절 위치로 낙상 규칙(heuristic)을 적용한다.
     - 예: 사람 박스가 '세로로 긺 -> 가로로 넓음'으로 급변하며 일정 시간 유지,
           또는 어깨/엉덩이 높이가 급격히 낮아짐.
  3) 낙상으로 판단되면 서버로 event_type="FALL" 이벤트만 전송(영상 X: 대역폭·프라이버시).

주의:
  - 160도 광각(어안) 렌즈는 가장자리 왜곡이 큼 -> 필요 시 왜곡보정(undistort) 후 추론.
  - 낙상 감지는 이 프로젝트에서 가장 어려운 부분. "관절 그리기"부터 화면에 띄운 뒤
    낙상 규칙을 붙이는 순서로 진행하세요.
"""


def detect_fall(keypoints) -> bool:
    """자세추정 결과(keypoints)로 낙상 여부 판단. (규칙은 팀이 구현)"""
    # TODO: 모델 선택 후 낙상 규칙 구현
    raise NotImplementedError("자세추정 모델 선택 후 낙상 규칙 구현")
